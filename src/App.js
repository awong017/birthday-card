import React from 'react';
import Styled from 'styled-components';

const AppDiv = Styled.div``

const App = () => {
  return (
    <AppDiv>
      App Component
    </AppDiv>
  );
}

export default App;
